# Project guidance for Claude Code

## Working preferences

- **Do not run lint, typecheck, build, or start the dev server unless explicitly asked.** Make the edits, leave verification for the user to trigger. They run a long-lived dev server themselves and prefer to invoke checks on demand.
